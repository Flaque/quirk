import React from "react";
import { Text } from "react-native";
import ScreenProps from "../ScreenProps";

export default class IndexLearnScreen extends React.Component<ScreenProps> {
  render() {
    return <Text>hix</Text>;
  }
}
